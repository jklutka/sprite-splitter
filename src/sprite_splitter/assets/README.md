# Sprite Splitter assets

Place the app logo here as:

- `app_logo.png` (recommended, square image, e.g. 512x512 or 768x768)

The app loads this image for:

- main window/application icon
- large logo in the About dialog

If the file is missing, the app falls back to a generated placeholder icon.
